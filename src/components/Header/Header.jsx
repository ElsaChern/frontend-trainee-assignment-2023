import SearchBar from "../SearchBar/SearchBar";
import HeaderContainer from "./slyled";

const Header = () => {
  return (
    <HeaderContainer>
      <SearchBar />
    </HeaderContainer>
  );
};

export default Header;
